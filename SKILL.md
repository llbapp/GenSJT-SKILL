---
name: GenSJT.skill
description: 情境判断测验(SJT)自动化命制。基于结构化母题与胜任特征辞典，为特定行业、岗位生成具有高心理测量学效度的测评题目。每次输出：纯净题目卷（Word）、完整答案卷（Word）、汇总表（Excel）。
---
# GenSJT.skill — 结构化命题执行引擎

## 一、 输入输出说明

### 输入参数

| 参数     | 是否必须 | 说明                                                     |
| :------- | :------- | :------------------------------------------------------- |
| 行业     | 必须     | 如：互联网、金融、制造业                                 |
| 岗位     | 必须     | 如：产品经理、销售、研发工程师                           |
| 维度     | 必须     | 必须严格匹配 `competence_dictionary.json` 中的维度名称 |
| 题量     | 可选     | 每维度默认 10 题                                         |
| 背景信息 | 可选     | 具体的业务痛点或公司文化，用于提升情境代入感             |

### 输出文档

| 文件名                        | 说明                                                       |
| ----------------------------- | ---------------------------------------------------------- |
| `测验卷_{行业}_{岗位}.docx` | 仅题目和选项，打乱后按位置从上到下用 A/B/C/D 标记          |
| `答案卷_{行业}_{岗位}.docx` | 保留测试卷的 A/B/C/D 标签，按 0→1→2→3 分排序 + 赋分说明 |
| `汇总表_{行业}_{岗位}.xlsx` | Sheet1：题目明细；Sheet2：维度描述；Sheet3：知识库引用记录 |

> **输出位置**：三份文档默认输出到**当前工作空间目录**。

#### 测试卷排版格式示例

```
1. 某航天器在运行期间，某关键作业窗口期因外部环境变化需要临时调整。
原定方案已通过各系统评审，你分管的某关键系统需要紧急重新核算窗口。
你会怎么做？
A 立即组织多系统联席会商，统筹评估后提出最优方案    ← 随机打乱后位置标记为 A
B 组织快速核算，争取在原窗口附近完成作业            ← 随机打乱后位置标记为 B
C 组织系统紧急复核，评估是否需要调整作业时机        ← 随机打乱后位置标记为 C
D 暂停该作业，等待环境条件稳定后再行评估            ← 随机打乱后位置标记为 D
```

#### 答案卷排版格式示例

（选项标签与测试卷一致，只是按分值 0→3 重新排序）

```
1. 某航天器在运行期间，某关键作业窗口期因外部环境变化需要临时调整。
原定方案已通过各系统评审，你分管的某关键系统需要紧急重新核算窗口。
你会怎么做？
C（0分）组织系统紧急复核，评估是否需要调整作业时机    ← 测试卷中是 C，0分
D（1分）暂停该作业，等待环境条件稳定后再行评估        ← 测试卷中是 D，1分
B（2分）组织快速核算，争取在原窗口附近完成作业        ← 测试卷中是 B，2分
A（3分）立即组织多系统联席会商，统筹评估后提出最优方案 ← 测试卷中是 A，3分

【赋分说明】
C=0分：该选项体现了专业审慎态度，在没有充分数据支撑前不轻易改变既定计划。
D=1分：该选项体现了安全第一的原则，但可能对后续业务节点产生一定影响。
B=2分：该选项兼顾了业务进度与安全考量，体现了在约束条件下寻求平衡的工作思路。
A=3分：该选项体现了全局视野和主动协调能力，能够在复杂情况下统筹各方、快速决策。
```

## 二、 核心命题逻辑流水线 (Pipeline)

**AI 必须严格按照以下顺序执行，严禁跳步：**

### Step 1：维度标准深度内化 (Target Alignment)

1. **检索**：在 `competence_dictionary.json` 中锁定用户输入的【维度】。若不存在匹配维度，输出"维度不存在"并**停止执行**。
2. **内化**：深度理解该维度的"定义（definition）"、"高分特征（high_score_features）"、"低分特征（low_score_features）"与"行为等级（behavior_levels）"。
3. **锚定**：将 3 分（最优）选项锁定在"优秀/良好"级行为，将 0 分（最差）选项锁定在"不足"级行为。

### Step 2：开发原理规则加载 (Principle Drive)

1. **研读**：加载 `SJT开发原理.md`，明确SJT设计原则。
2. **方法选择**：根据SJT开发的推荐做法编写题目。
3. **结构确认**：严格按照SJT开发原理.md中的结构要求编写题目，编写完成后，确认题目是否符合SJT设计原则。

### Step 3：参考母题与语言风格模仿 (Style Mimicking)

1. **母题提取**：从 `extracted_templates.json` 或 `competence_SJT.json` 中找到与该维度匹配的"抽象情境（abstract_scenario）"和"任务（management_task）"。
2. **样板参考**：在 `example_questions.json` 中检索同维度或同行业的例题，学习其专业术语的使用、冲突推进的节奏以及专业中性的语言风格。
3. **禁止**：严禁直接抄袭例题内容，参考语言风格。

### Step 4：情境渲染与选项映射 (Rendering & Mapping)

1. **渲染**：将 Step 3 选定的母题逻辑，穿上【行业】和【岗位】的皮肤。
2. **映射**：
   - **D选项 (3分)**：对应母题的 `best_practice` 且符合辞典"优秀"描述。
   - **C/B选项 (2/1分)**：对应母题的 `suboptimal_practice`。
   - **A选项 (0分)**：对应母题的 `worst_practice` 且符合辞典"不足"描述。

### Step 5: 参数估算逻辑对齐 (Metric Learning)

- **动作**：加载 `SJT_parameter_estimation.md`。掌握如何根据"情境模糊度"和"逻辑张力"推导 P 值和 D 值。
- **目标**：根据参数估计原则，对 Step 4 中的题目进行参数估算。得到 P 值和 D 值。

### 知识库引用清单

- `competence_dictionary.json`: 定义胜任特征标准。
- `competence_SJT.json`: 提供胜任特征与SJT情境的对应关系。
- `SJT开发原理.md`: 规定命题理论依据。
- `SJT_parameter_estimation.md`: 指导 P/D 参数的科学估算。
- `extracted_templates.json`: 提供带有占位符的命题模板。
- `example_questions.json`: 提供专业语言风格参考。

---

## 三、 编写规范 (Constraints)

### 1. 题干规范

* **长度**：100-180字（简练为首要原则，删除冗余背景描述，每句话必须推进情境）
* **语言**：用词精准，避免口语化表达；同一意思不重复说，不出现两次"你会怎么做"
* **结构**：背景（1-2句）→ 冲突/任务（2-3句）→ 结尾指导语（固定1句）
* **结尾**：固定为"面对这种情况，你会怎么做？"——**仅出现一次，作为题干末句，正文中绝对不允许再出现任何形式的指导语或提问语**
* **行业/岗位适配**：情境必须是该行业该岗位中真实可能发生的事，"你"必须有权限处理
* **特质激活**：情境应包含足够的模糊性（信息不完全、多重目标冲突），使得维度水平不同的人自然做出不同的行为选择

#### ⚠️ 题干常见错误（必须避免）

- ❌ 正文中出现"你该如何应对？""你会选择怎么做？"等提问，与结尾指导语重复
- ❌ 用多个近义句反复描述同一困境（如"进度严重滞后，项目面临极大延期风险，团队压力非常大"——三句说的是同一件事）
- ❌ 背景铺垫过长，超过题干总字数的50%
- ❌ 语言啰嗦，如"在这种情况下""此时此刻""面对如此复杂的局面"等空洞表达

### 2. 选项规范

- **字数极致对齐**：每个选项**严格 15-20 字**，4个选项字数差不得超过 3 个中文字。
- **等社会赞许性**：4个选项都必须是合理可接受的行为，差异在于策略/视野/方式，不能有明显的"道德对错"之分。任何凭常识就能选出高分的题目需要重写。
- **区分度**：选项间的行为差异应与目标维度直接相关，而非一般管理常识或智力水平的差异。

#### ⚠️ 选项常见错误（必须避免）

- ❌ 高分选项（3分）字数明显多于低分选项（如高分写30字、低分写12字）
- ❌ 用字数堆砌分值感（认为写得越多越像高分选项）
- ❌ 一个选项包含两个并列行为（如"先A再B，同时还要C"），导致字数超标

#### 选项编写核心原则
在生成每道题目时，必需检查是否符合以下三条核心规则
* **合理性**：选项行为在该情境下可能真实发生，"你"有权限和条件执行
* **相关性**：行为体现目标维度的相应水平，分值与维度水平严格对应
* **有效性**：行为在当前情境下有实际效果，效果程度与分值一致

### 3. 测量学参数估算

每题生成后，AI 必须基于以下逻辑进行参数估计：

- **P值 (难度)**：衡量 3 分选项的"隐蔽程度"。
  - 如果 3 分选项的行为非常符合社会直觉（容易选对），P值设为 0.70-0.90（容易）。
  - 如果 3 分选项需要深度的管理洞察且与其他干扰项极难区分，P值设为 0.20-0.40（困难）。
- **D值 (区分度)**：衡量 3 分与 0 分行为的逻辑张力。
  - 若逻辑完全遵循 `best/worst_practice` 对比，D值标记为 0.40 以上（优秀）。
  - **强制规则**：若 D < 0.30，该题必须重写。

---

## 四、 执行流与自动化

### 执行指令
#### 1. 密码验证与解密 + 定向知识库检索（合并为一步）

询问密码后，执行以下命令（同时完成解密和按维度检索知识库）：

```bash
python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" query --dimensions <维度1>,<维度2>,...
```

该命令会：
1. 验证密码并解密参考资料
2. 按指定维度从所有知识库中**精准检索**相关内容
3. 输出一个精简的 JSON，包含：
   - `dimensions`：目标维度的定义、高低分特征、行为等级
   - `templates`：匹配维度的母题模板（含占位符骨架和逻辑分析）
   - `competence_sjt`：匹配维度的情境-任务-行为锚点
   - `examples`：匹配维度的例题（每维度最多5道，用于语言风格参考）
   - `parameter_guide`：P/D 参数估算指南全文

**AI 必须基于此 JSON 输出进行命题，不得自行读取原始知识库文件。**

#### 2. 分批生成逻辑
- 当总题量 > 20题时，必须分批生成，每批不超过 10 题。
- 每批完成后立即存入 `temp_items.json`，全部批次完成后再统一生成文档。
- 生成的 JSON 必须包含 `template_id` 以便溯源。
- 格式必须匹配以下 JSON 数据格式规范。

#### 3. JSON 数据格式规范

```json
{
  "dimension": "<维度名称>",
  "stem": "<100-180字的情境题干，结尾为"面对这种情况，你会怎么做？">",
  "options": {
    "A": {"text": "<15-20字行为描述>", "score": <0-3整数>, "reason": "<赋分说明，50-100字>"},
    "B": {"text": "<15-20字行为描述>", "score": <0-3整数>, "reason": "<赋分说明，50-100字>"},
    "C": {"text": "<15-20字行为描述>", "score": <0-3整数>, "reason": "<赋分说明，50-100字>"},
    "D": {"text": "<15-20字行为描述>", "score": <0-3整数>, "reason": "<赋分说明，50-100字>"}
  },
  "p_value": "<0.00-1.00浮点数，答对率估算>",
  "p_level": "<容易 | 中等 | 困难>",
  "d_value": "<0.00-1.00浮点数，区分度估算>",
  "d_level": "<优秀 | 良好 | 较差>",
  "p_d_reason": "<P/D估算理由，50-80字，说明为什么给出这样的难度和区分度判断>"
}
```

## 五、 文件生成

所有题目生成完毕后，调用（在工作空间目录下执行，文档将输出到该目录）：

```bash
cd <工作空间路径> && python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" gen_docs
```

也可显式指定输出目录：

```bash
python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" gen_docs --output-dir <路径>
```


## 六、 质量质检清单 

每道题生成后对以下清单逐条验证

- [ ] 情境100-180字，结尾**仅出现一次**"面对这种情况，你会怎么做？"
- [ ] 情境语言简练，无重复描述、无冗余背景、无空洞表达
- [ ] 情境正文中**无任何提问语或指导语**（如"你会怎么做""应该如何处理"等）
- [ ] 情境在该行业/岗位中真实可发生，"你"有权限处理
- [ ] 情境能激活目标维度的个体差异（有模糊性，无明显线索）
- [ ] **4个选项均在15-20字之间，字数差距≤3字**（逐项统计字数确认）
- [ ] 三条核心规则全部满足（合理性、相关性、有效性）
- [ ] 4个选项等社会赞许性，无法凭常识判断对错
- [ ] 各选项行为差异与维度水平直接相关（区分度）
- [ ] 赋分说明包含维度特征+情境效果+分值依据
- [ ] **已给出P值（难度）和D值（区分度）估算，含估算理由**
- [ ] D值 ≥ 0.30，否则重写题目