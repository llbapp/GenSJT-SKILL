---
name: gensjt
description: |
  情境判断测验(SJT)自动化命题引擎。基于胜任特征辞典与例题库，为指定行业、岗位生成具有心理测量学效度的 SJT 题目，输出测验卷(Word)、答案卷(Word)、汇总表(Excel)。
  当用户要求出SJT题目、生成情境判断测验、帮我出测评题、SJT命制、出题、命题，或提到行业+岗位+维度组合出题时，触发此技能。
version: 3.4.0
author: llbapp
---

# GenSJT.skill — 结构化命题执行引擎

## 一、 输入输出说明

### 输入参数

| 参数   | 是否必须 | 说明                                         |
| :--- | :--- | :----------------------------------------- |
| 行业   | 必须   | 如：互联网、金融、制造业                               |
| 岗位   | 必须   | 如：产品经理、销售、研发工程师                            |
| 维度   | 必须   | 必须严格匹配 `competence_dictionary.json` 中的维度名称 |
| 题量   | 可选   | 每维度默认 10 题                                 |
| 背景信息 | 可选   | 具体的业务痛点或公司文化，用于提升情境代入感                     |

### 输出文档

| 文件名                  | 说明                                     |
| -------------------- | -------------------------------------- |
| `测验卷_{行业}_{岗位}.docx` | 仅题目和选项，打乱后按位置从上到下用 A/B/C/D 标记          |
| `答案卷_{行业}_{岗位}.docx` | 保留测试卷的 A/B/C/D 标签，按 0→1→2 分排序 + 赋分说明   |
| `汇总表_{行业}_{岗位}.xlsx` | Sheet1：题目明细；Sheet2：维度描述；Sheet3：知识库引用记录 |

> **输出位置**：三份文档默认输出到**当前工作空间目录**。

#### 测试卷排版格式示例

```
1. 某航天器在运行期间，某关键作业窗口期因外部环境变化需要临时调整。
原定方案已通过各系统评审，你分管的某关键系统需要紧急重新核算窗口。
你会怎么做？
A 立即组织多系统联席会商，统筹评估后提出最优方案    ← 随机打乱后位置标记为 A
B 组织快速核算，争取在原窗口附近完成作业            ← 随机打乱后位置标记为 B
C 组织系统紧急复核，评估是否需要调整作业时机        ← 随机打乱后位置标记为 C
D 暂停该作业，等待环境条件稳定后再行评估            ← 随机打乱后位置标记为 D
```

#### 答案卷排版格式示例

（选项标签与测试卷一致，只是按分值 0→2 重新排序）

```
1. 某航天器在运行期间，某关键作业窗口期因外部环境变化需要临时调整。
原定方案已通过各系统评审，你分管的某关键系统需要紧急重新核算窗口。
你会怎么做？
C（0分）暂停该作业，等待环境条件稳定后再行评估        ← 测试卷中是 C，0分
B（1分）组织系统紧急复核，评估是否需要调整作业时机    ← 测试卷中是 B，1分
D（1分）组织快速核算，争取在原窗口附近完成作业        ← 测试卷中是 D，1分
A（2分）立即组织多系统联席会商，统筹评估后提出最优方案 ← 测试卷中是 A，2分

【赋分说明】
C=0分：该选项体现了保守态度，缺乏主动应对意识，可能对后续业务节点产生严重影响。
B=1分：该选项体现了审慎态度，能识别问题但视野局限于本系统，缺乏跨系统协调意识。
D=1分：该选项兼顾了业务进度与安全考量，体现了在约束条件下寻求平衡的工作思路。
A=2分：该选项体现了全局视野和主动协调能力，能够在复杂情况下统筹各方、快速决策。
```

## 二、 核心命题逻辑流水线 (Pipeline)

**AI 必须严格按照以下顺序执行，严禁跳步：**

### Step 1：维度标准深度内化 (Target Alignment)

> **数据来源**：`query` 命令已由程序完成维度检索，AI **直接读取** JSON 输出中的字段，不得自行判断维度有效性或查询原始知识库文件。

1. **确认有效维度**：读取 `valid_dimensions` 列表，**仅对其中的维度执行后续 Step**。`skipped_dimensions` 已由程序拒绝，**AI 绝对不得为其生成任何题目**。

2. **逐维度内化**：对每个 `valid_dimensions` 中的维度，从 `dimensions.<维度名>` 字段中读取以下信息并深度理解：

   | 字段 | 含义 | 命题用途 |
   |---|---|---|
   | `definition` | 维度定义 | 明确该维度考察的核心心理构念 |
   | `high_score_features` | 高分特征 | 对应 2 分选项的行为模式 |
   | `low_score_features` | 低分特征 | 对应 0 分选项的行为模式 |
   | `behavior_levels.优秀` | 优秀级行为描述 | 2 分选项的具体行为参照 |
   | `behavior_levels.良好` | 良好级行为描述 | 可作为 2 分或偏高 1 分选项参照 |
   | `behavior_levels.中等` | 中等级行为描述 | 对应 1 分选项（行为有价值但视野受限） |
   | `behavior_levels.欠佳` | 欠佳级行为描述 | 对应另一个 1 分选项（被动/局部应对） |
   | `behavior_levels.不足` | 不足级行为描述 | 0 分选项的具体行为参照 |

3. **锚定分值**：
   - **2 分（唯一）**：锁定在 `behavior_levels.优秀` 描述的行为模式，与 `high_score_features` 一致
   - **1 分（两个）**：分别对应 `behavior_levels.中等` 和 `behavior_levels.欠佳`，两者行为角度不同但均为部分有效应对
   - **0 分（唯一）**：锁定在 `behavior_levels.不足` 描述的行为模式，与 `low_score_features` 一致

### Step 2：开发原理规则加载 (Principle Drive)

1. **研读**：加载 `SJT开发原理.md`，明确SJT设计原则。
2. **方法选择**：根据SJT开发的推荐做法编写题目。
3. **结构确认**：严格按照SJT开发原理.md中的结构要求编写题目，编写完成后，确认题目是否符合SJT设计原则。

### Step 3：例题改编与语言风格内化 (Example-Based Adaptation)

**核心思路：以** **`example_questions.json`** **中的例题为第一改编样本，对其进行行业/岗位换壳，而非从零创作。**

1. **例题选取**：从 `examples` 字段中找到与目标维度匹配的例题（每维度最多5道）。优先选择情境冲突最清晰、选项行为差异最明确的那道。
2. **改编方式**：
   - **保留**：原题的情境冲突结构、行为决策逻辑、各选项的分值层级关系
   - **替换**：行业背景、岗位角色、涉及的人物/产品/业务场景，换成目标行业和岗位的对应要素
   - **调整**：选项文字改为该行业岗位的惯用表达（参考 `job_context.domain_vocabulary`），确保语言专业、情境真实
3. **补充参考**：若该维度例题数量不足（< 2道）或改编后情境重复，可从 `competence_sjt` 字段中提取"抽象情境（abstract\_scenario）"和"行为锚点（behavioral\_anchors）"作为补充创作素材。
4. **禁止**：严禁直接照抄例题原文的情境描述和选项文字；人名、公司名、具体产品名必须替换。

### Step 4：情境渲染与选项映射 (Rendering & Mapping)

1. **岗位锚定**：从 `job_context.results` 中提取目标岗位的：
   - `typical_work_actions`：确保情境中的行为在该岗位的职责范围内（参考 `role_boundary`）
   - `conflict_source`：核心冲突应与该岗位的典型矛盾一致
   - `stakeholders`：情境中的其他角色应是该岗位真实的利益相关方
   - `domain_vocabulary`：情境中应自然使用行业专业术语
   - `exclusive_scenarios`：参考行业独有情境的逻辑深度和复杂度
2. **情境渲染**：将 Step 3 选定的例题情境逻辑，替换为目标【行业】和【岗位】的具体场景。保留原题的冲突结构，不改变行为决策层次。
3. **选项映射**：以例题的 `option_scores` 为分值参照，将原选项行为逻辑迁移到新岗位场景，并统一映射到 **2-1-1-0 分制**：
   - **2分选项（唯一）**：对应原题最高分选项的行为模式，且符合胜任特征辞典"优秀"描述
   - **1分选项（两个）**：对应原题中间分值选项的行为模式，体现"中等/欠佳"级别，两者行为角度不同但效果相近
   - **0分选项（唯一）**：对应原题最低分选项的行为模式，且符合胜任特征辞典"不足"描述

### Step 5: 参数估算逻辑对齐 (Metric Learning)

- **动作**：加载 `SJT_parameter_estimation.md`。掌握如何根据"情境模糊度"和"逻辑张力"推导 P 值和 D 值。
- **目标**：根据参数估计原则，对 Step 4 中的题目进行参数估算。得到 P 值和 D 值。

### 知识库引用清单

- `competence_dictionary.json`: 定义胜任特征标准。
- `competence_SJT.json`: 提供胜任特征与SJT情境的对应关系（例题不足时的补充素材）。
- `SJT开发原理.md`: 规定命题理论依据。
- `SJT_parameter_estimation.md`: 指导 P/D 参数的科学估算。
- `example_questions.json`: **核心改编样本**，提供各维度真实例题（含完整情境、选项文字和分值）。

***

## 三、 编写规范 (Constraints)

### 1. 题干规范

- **长度**：100-180字（简练为首要原则，删除冗余背景描述，每句话必须推进情境）
- **语言**：用词精准，避免口语化表达；同一意思不重复说，不出现两次"你会怎么做"
- **结构**：背景（1-2句）→ 冲突/任务（2-3句）→ 结尾指导语（固定1句）
- **结尾**：固定为"面对这种情况，你会怎么做？"——**仅出现一次，作为题干末句，正文中绝对不允许再出现任何形式的指导语或提问语**
- **行业/岗位适配**：情境必须是该行业该岗位中真实可能发生的事，"你"必须有权限处理
- **特质激活**：情境应包含足够的模糊性（信息不完全、多重目标冲突），使得维度水平不同的人自然做出不同的行为选择

#### ⚠️ 题干常见错误（必须避免）

- ❌ 正文中出现"你该如何应对？""你会选择怎么做？"等提问，与结尾指导语重复
- ❌ 用多个近义句反复描述同一困境（如"进度严重滞后，项目面临极大延期风险，团队压力非常大"——三句说的是同一件事）
- ❌ 背景铺垫过长，超过题干总字数的50%
- ❌ 语言啰嗦，如"在这种情况下""此时此刻""面对如此复杂的局面"等空洞表达

### 2. 选项规范

- **字数极致对齐**：每个选项**严格 15-20 字**，4个选项字数差不得超过 3 个中文字。
- **等社会赞许性**：4个选项都必须是合理可接受的行为，差异在于策略/视野/方式，不能有明显的"道德对错"之分。任何凭常识就能选出高分的题目需要重写。
- **区分度**：选项间的行为差异应与目标维度直接相关，而非一般管理常识或智力水平的差异。

#### ⚠️ 选项常见错误（必须避免）

- ❌ 高分选项（2分）字数明显多于低分选项（如高分写30字、低分写12字）
- ❌ 用字数堆砌分值感（认为写得越多越像高分选项）
- ❌ 一个选项包含两个并列行为（如"先A再B，同时还要C"），导致字数超标

#### 选项编写核心原则

在生成每道题目时，必需检查是否符合以下三条核心规则

- **合理性**：选项行为在该情境下可能真实发生，"你"有权限和条件执行
- **相关性**：行为体现目标维度的相应水平，分值与维度水平严格对应
- **有效性**：行为在当前情境下有实际效果，效果程度与分值一致

### 3. 测量学参数估算

每题生成后，AI 必须基于以下逻辑进行参数估计：

- **P值 (难度)**：衡量 2 分选项的"隐蔽程度"。
  - 如果 2 分选项的行为非常符合社会直觉（容易选对），P值设为 0.70-0.90（容易）。
  - 如果 2 分选项需要深度的管理洞察且与其他干扰项极难区分，P值设为 0.20-0.40（困难）。
- **D值 (区分度)**：衡量 2 分与 0 分行为的逻辑张力。
  - 若逻辑对比鲜明，两者行为差异直接体现维度核心特征，D值标记为 0.40 以上（优秀）。
  - **强制规则**：若 D < 0.30，该题必须重写。

***

## 四、 执行流与自动化

### 执行指令

**AI 必须严格按照以下两阶段顺序执行：**

#### 阶段一：密码验证（最先执行）

1. **立即询问用户密码**，不等待用户提供其他参数。
2. 获取密码后，执行验证命令（此时不需要维度参数）：

```bash
python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" verify
```

1. 根据返回结果：
   - **验证成功**：告知用户"密码验证通过"，然后进入阶段二收集参数。
   - **验证失败**：告知用户"密码错误，请重新输入"，重新询问密码。**连续 3 次失败后停止执行。**

> **密码验证通过后，密码已在内存中保存，后续** **`query`** **和** **`gen_docs`** **命令复用同一密码，无需再次询问。**

#### 阶段二：参数收集 + 定向知识库检索

密码验证通过后，再向用户收集业务参数（行业、岗位、维度、题量、背景信息）。

参数收集完成后，执行以下命令（同时完成解密、维度检索和行业岗位模糊搜索）：

```bash
python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" query \
  --dimensions <维度1>,<维度2>,... \
  --industry <行业> \
  --position <岗位>
```

该命令会：

1. 验证密码并解密参考资料
2. 按指定维度从所有知识库中**精准检索**相关内容
3. 按用户输入的行业和岗位在 `industry_job_context_db.json` 中**模糊搜索**匹配的行业岗位背景信息
4. 输出一个精简的 JSON，包含：
   - `valid_dimensions`：**程序已验证的有效维度列表**（严格精确匹配胜任特征辞典）。**AI 只能对此列表中的维度出题，任何不在此列表中的维度一律不得出题。**
   - `skipped_dimensions`：**无效维度列表**（程序在字典中找不到精确匹配）。程序已拒绝这些维度的知识库检索，AI **严禁**为其生成任何题目，必须在开始出题前告知用户。
   - `dimensions`：目标维度的完整结构化信息（**Step 1 内化的核心数据来源**），每个维度包含：
     - `definition`：维度定义
     - `high_score_features` / `low_score_features`：高低分行为特征
     - `behavior_levels`：行为等级描述（优秀 / 良好 / 中等 / 欠佳 / 不足）
   - `examples`：匹配维度的例题（每维度最多5道，**Step 3 核心改编样本**）
   - `competence_sjt`：匹配维度的情境-任务-行为锚点（例题不足时补充）
   - `parameter_guide`：P/D 参数估算指南全文
   - `job_context`：**行业岗位匹配结果**（可选，传入 --industry 和 --position 时存在）
     - `industry_match`：行业匹配列表（名称 + 相似度分数）
     - `position_match`：岗位匹配列表（名称 + 相似度分数）
     - `results`：综合匹配 Top 3 记录，每条包含完整岗位背景信息：
       - `logic.typical_work_actions`：典型工作行为
       - `logic.role_boundary`：职责边界（该做什么 / 不该做什么）
       - `logic.conflict_source`：核心冲突来源
       - `logic.stakeholders`：利益相关方
       - `exclusive_scenarios`：行业独有情境及专业应对逻辑
       - `domain_vocabulary`：行业专业术语
       - `_meta`：匹配元数据（综合分、行业分、岗位分）

**AI 命题时必须深度参考** **`job_context.results`** **中的岗位信息，确保情境真实感、角色边界正确、术语地道。** 不得自行读取原始知识库文件。

#### 阶段三：维度有效性确认（不得跳过）

> **维度匹配完全由程序完成，AI 不做任何维度有效性判断。**

- 读取 `valid_dimensions`：这是程序已严格校验的有效维度列表，**只对这些维度出题**。
- 读取 `skipped_dimensions`：这是程序已拒绝的无效维度，**无论如何都不出题**。若该列表非空，**必须在开始出题前**向用户说明哪些维度无效（未在胜任特征辞典中找到），以及这些维度不会生成题目。
- 若 `valid_dimensions` 为空（程序已以非零退出码终止），**停止执行**，不调用 `gen_docs`。

#### 阶段四：分批生成逻辑

- 当总题量 > 20题时，必须分批生成，每批不超过 10 题。
- 每批完成后立即存入 `temp_items.json`，全部批次完成后再统一生成文档。
- 生成的 JSON 必须包含 `example_id`（改编所参照的例题 id）以便溯源。
- 格式必须匹配以下 JSON 数据格式规范。

#### 3. JSON 数据格式规范

```json
{
  "dimension": "<维度名称>",
  "example_id": <改编参照的例题 id，整数>,
  "stem": "<100-180字的情境题干，结尾为"面对这种情况，你会怎么做？">",
  "options": {
    "A": {"text": "<15-20字行为描述>", "score": <0|1|2>, "reason": "<赋分说明，50-100字>"},
    "B": {"text": "<15-20字行为描述>", "score": <0|1|2>, "reason": "<赋分说明，50-100字>"},
    "C": {"text": "<15-20字行为描述>", "score": <0|1|2>, "reason": "<赋分说明，50-100字>"},
    "D": {"text": "<15-20字行为描述>", "score": <0|1|2>, "reason": "<赋分说明，50-100字>"}
  },
  "score_distribution": "2-1-1-0（必须恰好1个2分、2个1分、1个0分，不可出现其他分值）",
  "p_value": "<0.00-1.00浮点数，2分选项的答对率估算>",
  "p_level": "<容易 | 中等 | 困难>",
  "d_value": "<0.00-1.00浮点数，区分度估算>",
  "d_level": "<优秀 | 良好 | 较差>",
  "p_d_reason": "<P/D估算理由，50-80字，说明为什么给出这样的难度和区分度判断>"
}
```

## 五、 文件生成

所有题目生成完毕后，调用（在工作空间目录下执行，文档将输出到该目录）：

```bash
cd <工作空间路径> && python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" gen_docs
```

也可显式指定输出目录：

```bash
python3 ~/.workbuddy/skills/GenSJT.skill/gensjt.py "$PASSWORD" gen_docs --output-dir <路径>
```

## 六、 质量质检清单

每道题生成后对以下清单逐条验证

- [ ] 情境100-180字，结尾**仅出现一次**"面对这种情况，你会怎么做？"
- [ ] 情境语言简练，无重复描述、无冗余背景、无空洞表达
- [ ] 情境正文中**无任何提问语或指导语**（如"你会怎么做""应该如何处理"等）
- [ ] 情境在该行业/岗位中真实可发生，"你"有权限处理
- [ ] 情境能激活目标维度的个体差异（有模糊性，无明显线索）
- [ ] **4个选项均在15-20字之间，字数差距≤3字**（逐项统计字数确认）
- [ ] 三条核心规则全部满足（合理性、相关性、有效性）
- [ ] 4个选项等社会赞许性，无法凭常识判断对错
- [ ] 各选项行为差异与维度水平直接相关（区分度）
- [ ] 赋分说明包含维度特征+情境效果+分值依据
- [ ] **已给出P值（难度）和D值（区分度）估算，含估算理由**
- [ ] D值 ≥ 0.30，否则重写题目

